# Testing Strategy

Test one small feature at a time.

Initial tests:

- x0 is always zero
- PC starts at 0
- Memory can be created
- Memory read/write works
- ADDI works

Each instruction should have its own tests.

Every bug should get a regression test.